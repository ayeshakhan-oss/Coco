# Makes scripts.evals an importable package so the webapp can do
#   from scripts.evals.candidate_communication_eval import evaluate_email
# (This file was missing, which broke the import from outside scripts/.)
