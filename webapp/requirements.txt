# Backend (FastAPI) service dependencies — installed in the backend Railway
# service / local venv. The reused scripts/utils modules are stdlib-only;
# scripts/evals/fetch_submission_corpora.py (case-study extraction) is not --
# see the python-pptx/openpyxl/PyMuPDF entries below.

fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
psycopg[binary]>=3.2          # psycopg v3 — has Python 3.14 wheels (psycopg2 does not)
alembic>=1.13
pydantic-settings>=2.5
google-auth>=2.35             # server-side ID-token verification
google-api-python-client>=2.140  # Gmail readonly evidence sync (read-only)
google-auth-httplib2>=0.2     # AuthorizedHttp transport (batched Gmail fetch + timeout)
httplib2>=0.22                # socket timeout for the Gmail client
requests>=2.32                # transport used by google-auth's token verifier
pyjwt>=2.9                    # signed session cookie
anthropic>=0.40               # Claude API (AI drafting)
apscheduler>=3.10             # in-app hourly Gmail evidence sync
httpx>=0.27
python-dotenv>=1.0
pypdf>=5.0                    # CV text extraction for CV-stage drafting evidence
python-docx>=1.1              # CV text extraction (.docx resumes); also case-study .docx
python-pptx>=1.0              # case-study .pptx extraction (fetch_submission_corpora.py)
openpyxl>=3.1                 # case-study .xlsx extraction (values AND formulas)
PyMuPDF>=1.24                 # case-study .pdf extraction (import name: fitz)
