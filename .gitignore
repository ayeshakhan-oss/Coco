# CREDENTIALS & SECRETS (never track)
# NOTE: .gitignore does NOT support inline comments — keep comments on their own lines.
.env
.env.local
credentials.json
token.json
token_gmail.json
token_gmail_labels.json
token_sheets.json
token_sheets_broad.json
.mcp.json
# Service account JSON files (Google Cloud, etc.) — hyphenated key files
*-*.json
# ...but DO track the frontend lockfile (npm ci needs it; matched by *-*.json above)
!frontend/package-lock.json
# OAuth tokens / service-account keys anywhere under tools/ and .claude/config/
tools/*.json
.claude/config/*.json
.claude/config/*token*
*service-account*.json
*serviceaccount*.json

# COMPILED & CACHE (never track)
__pycache__/
*.pyc
*.pyo
*.egg-info/

# PYTHON VIRTUAL ENVS (never track)
.venv/
venv/
env/

# WEBAPP — NODE / FRONTEND BUILD (never track)
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# TEMPORARY & LOGS (never track)
Temp/
*.bak
*.log
logs/

# CANDIDATE DATA & DERIVED FILES (sensitive, never track)
output/
data/

# SPECIFIC TEMPORARY FILES (not blanket rules)
job36_rejection_replies.json
job36_case_study_gmail_check.json
job32_invite_check.txt
job36_invite_check.txt
nain_tara_cv.pdf

# AD-HOC CANDIDATE PILOT / SEND SCRIPTS (contain candidate names + emails = PII, never track)
scripts/send_bilal_ahmed_warm_bench_pilot.py
scripts/send_cpd_coach_values_invite_pilot.py
scripts/send_dummy_candidate_comms_pilot.py
scripts/send_growth_manager_values_invite_pilot.py
scripts/send_noor_ul_ain_warm_bench_cpd_pilot.py
scripts/send_tehreem_gulalai_gwc_rejection_draft.py
scripts/send_tehreem_gulalai_gwc_rejection_pilot.py
scripts/tehreem_gulalai_gwc_rejection_pilot.html
scripts/send_role_closure_pilot.py
scripts/send_interview_reminder_pilot.py

# VENDORED THIRD-PARTY UI/UX SKILL (local-only, reinstall via: npx uipro-cli init --ai claude)
.claude/skills/ui-ux-pro-max/
